version: 2

models:
    - name: stg_suppliers
      description: '{{ doc("orders_docs") }}'
      columns:
        - name: supplier_id
          description: Unique ID for this table
          tests:
            - not_null
            - unique